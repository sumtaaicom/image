# Railway 배포용 환경 변수 템플릿
# ================================
# Railway 대시보드에서 이 변수들을 설정하세요

# =====================
# n8n 서비스 환경변수
# =====================

# n8n 기본 설정
N8N_HOST=0.0.0.0
N8N_PORT=5678
N8N_PROTOCOL=https
# Railway 도메인으로 변경하세요 (예: https://lg-image-n8n.railway.app/)
WEBHOOK_URL=https://<your-n8n-railway-domain>/

# PostgreSQL 연결 (Railway 변수 참조)
# Railway에서 PostgreSQL 애드온 추가 후 자동으로 사용 가능
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=${{Postgres.PGHOST}}
DB_POSTGRESDB_PORT=${{Postgres.PGPORT}}
DB_POSTGRESDB_DATABASE=${{Postgres.PGDATABASE}}
DB_POSTGRESDB_USER=${{Postgres.PGUSER}}
DB_POSTGRESDB_PASSWORD=${{Postgres.PGPASSWORD}}

# n8n 보안 설정 (선택사항 - 관리자 페이지 보호)
# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=<strong-password>

# =====================
# API 키 설정 (필수)
# =====================

# Google Gemini API (필수 - 이미지 합성용)
GEMINI_API_KEY=your-gemini-api-key

# Jina AI API (권장 - 이미지 추출 성능 향상)
JINA_API_KEY=your-jina-api-key

# =====================
# 추가 API 키 (선택사항)
# =====================

# Replicate API (FLUX 모델 사용 시)
REPLICATE_API_KEY=

# OpenAI API (DALL-E 사용 시)
OPENAI_API_KEY=

# Stability AI API
STABILITY_API_KEY=

# ==========================
# 프론트엔드 서비스 환경변수
# ==========================

# n8n 웹훅 URL (프론트엔드 Nginx가 프록시할 주소)
# Railway 내부 네트워크 사용 시: http://<n8n-service-name>:5678/webhook/
# 외부 URL 사용 시: https://<n8n-railway-domain>/webhook/
N8N_WEBHOOK_URL=https://<your-n8n-railway-domain>/webhook/
